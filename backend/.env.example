PORT=4000

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=luxia
DB_USER=postgres
DB_PASSWORD=

# Authentication
JWT_SECRET=change-me
ADMIN_EMAIL=concierge@luxia.local
# ADMIN_PASSWORD_HASH= # optional bcrypt hash, defaults to LuxiaAdmin2024!

# API Keys Encryption
# CRITICAL: Generate a secure encryption key for production using:
# npm run generate-key
# This key is used to encrypt/decrypt API keys stored in the database
# NEVER commit this key to version control
# NEVER use the default key in production
# Store this securely (e.g., in your hosting platform's environment variables)
ENCRYPTION_KEY=

# Email Notifications (optional)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=username
# SMTP_PASSWORD=password
NOTIFY_FROM="Luxia Rituals <no-reply@luxia.local>"

# SMS Notifications (optional)
# SMS_WEBHOOK_URL=https://sms.example.com/send
# SMS_API_KEY=your-api-key
# SMS_FROM=+15551234567
